import { PIE_GREY_IMAGE } from 'ui/components/VisualiseIcon/assets';

export const title = 'What is the completion progress for assignments?';
export const description = 'What is the completion progress for assignments?';
export const image = PIE_GREY_IMAGE;
